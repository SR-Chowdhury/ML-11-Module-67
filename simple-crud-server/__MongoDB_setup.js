/**
 * ----------------------------------------------------------------
 *                          MongoDB setup
 * ----------------------------------------------------------------
 * step 1: creat or sign in MongoDB
 * step 2: create an user with password
 * step 3: whitelist IP address
 * step 4: database > connect > driver > Node > View full code > copy the code and paste into server (express / node ) side
 * step 5: change the password in the URI * 
 * 
 * 
 */